import { View, Text } from 'react-native';

// TODO (T24): listagem consolidada de reservas (ADMIN)
export default function AdminReservationsScreen() {
  return (
    <View>
      <Text>Todas as Reservas</Text>
    </View>
  );
}
